@model IEnumerable<ClothingStoreWeb.Models.Product>
@{
    ViewData["Title"] = "Quản lý Sản phẩm";
    var currentPage = (int)(ViewBag.CurrentPage ?? 1);
    var totalPages = (int)(ViewBag.TotalPages ?? 1);
}

<div class="mt-4">
    <h2>Danh sách Sản phẩm</h2>
    
    <div class="d-flex flex-wrap justify-content-between align-items-center mb-3 gap-3">
        <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Thêm sản phẩm mới</a>
        
        <form method="get" class="d-flex gap-2 align-items-center" style="max-width: 400px; width: 100%;">
            <div class="input-group">
                <span class="input-group-text bg-white"><i class="bi bi-search text-muted"></i></span>
                <input type="text" name="search" value="@ViewBag.Search" class="form-control" placeholder="Tìm kiếm sản phẩm..." style="font-size: 0.9rem;" />
                @if (!string.IsNullOrEmpty(ViewBag.Search))
                {
                    <a asp-action="Index" class="btn btn-outline-secondary d-flex align-items-center justify-content-center" title="Xóa lọc"><i class="bi bi-x-lg"></i></a>
                }
                <button type="submit" class="btn btn-dark fw-bold">Tìm</button>
            </div>
        </form>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <table class="table table-bordered table-hover align-middle">
        <thead class="table-dark">
            <tr>
                <th>Mã</th>
                <th>Tên sản phẩm</th>
                <th>Danh mục</th>
                <th>Giá gốc</th>
                <th>Quản lý chi tiết</th>
                <th>Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.ProductID</td>
                    <td class="fw-bold">@item.Name</td>
                    <td>@item.Category?.Name</td>
                    <td class="text-danger fw-bold">@item.BasePrice.ToString("N0") đ</td>
                    <td>
                        <a asp-controller="ProductImage" asp-action="Index" asp-route-productId="@item.ProductID" class="btn btn-sm btn-info text-white">
                            <i class="bi bi-images"></i> Ảnh
                        </a>
                        <a asp-controller="ProductVariant" asp-action="Index" asp-route-productId="@item.ProductID" class="btn btn-sm btn-success">
                            <i class="bi bi-box-seam"></i> Tồn kho
                        </a>
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ProductID" class="btn btn-sm btn-warning">Sửa</a>
                        <a asp-action="Delete" asp-route-id="@item.ProductID" class="btn btn-sm btn-danger">Xóa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @if (totalPages > 1)
    {
        <nav aria-label="Phan trang san pham admin" class="mb-3">
            <ul class="pagination">
                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="Index" asp-route-page="@(currentPage - 1)" asp-route-search="@ViewBag.Search">Trước</a>
                </li>
                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(i == currentPage ? "active" : "")">
                        <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-search="@ViewBag.Search">@i</a>
                    </li>
                }
                <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="Index" asp-route-page="@(currentPage + 1)" asp-route-search="@ViewBag.Search">Sau</a>
                </li>
            </ul>
        </nav>
    }
    
    <a asp-controller="Admin" asp-action="Index" class="btn btn-secondary">Quay lại Dashboard</a>
</div>